/**
 * Write a function that, given a number `n`,
 * returns an array that comprises the digits of that number.
 * 
 * Examples:
 * createDigitsArray(123) -> [1, 2, 3]
 * createDigitsArray(0) -> [0]
 * createDigitsArray(389741982) -> [3, 8, 9, 7, 4, 1, 9, 8, 2]
 */
function createDigitsArray(n) {

}

/**
 * Write a function that, given a number `n`, determines
 * whether or not it is a Keith number.
 */
function isKeithNumber(n) {

}